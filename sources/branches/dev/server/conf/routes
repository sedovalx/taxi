# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                     controllers.Application.index(path = "")
GET        /api/db/schema        controllers.SchemaController.getDdlScript

# User routes
GET        /api/users           controllers.UserController.all

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file         controllers.Assets.at(path="/public/assets", file)

# Every other page
GET        /*path                controllers.Application.index(path)